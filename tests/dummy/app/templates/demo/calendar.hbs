{{paper-datetime-picker datetime=date onChange=(action (mut date))}}

{{#paper-datetime-picker datetime=date onChange=(action (mut date)) as |picker|}}
  {{picker.input}}

  {{#picker.calendar as |calendar|}}
    {{calendar.nav}}
    {{calendar.days minDate=minDatetime maxDate=maxDatetime}}
  {{/picker.calendar}}
{{/paper-datetime-picker}}
